package:
  name: bns
  version: "1.3"

requirements:
  host:
    - {{ compiler('cxx') }} # [linux or arm64]
    #    - clang =15.* # [osx and x]
    #- clangxx =15.* # [osx and x]
    - zlib # [linux or arm64]

test:
  commands:
    - bns


source:
  url: https://people.kth.se/~dubrova/BNS/bns_v1.3.zip
